name: CI

on:
  push:
    branches:
      - development

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        node-version: 10.x

    - name: Install dependencies
      run: npm install
    - name: Deploy page
      run: npm run deploy
